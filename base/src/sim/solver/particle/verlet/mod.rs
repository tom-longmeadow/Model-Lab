// use crate::sim::{solver::particle::ParticleSolver, storage::AosStorage};

// pub mod aos;

// /// Marker supertrait for all AoS verlet integrators.
// /// Enforces that every AoS verlet solver is also a ParticleSolver.
// pub trait AosVerletSolver<S>: ParticleSolver<S>
// where
//     S: AosStorage,
// {}