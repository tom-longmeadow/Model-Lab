[package]
name = "base"
version = "0.1.0"
edition = "2024"

[features]
testing = []

[dependencies]
derive_more = { workspace = true, features = ["from", "into", "display", "as_ref"] }
strum = { workspace = true }

